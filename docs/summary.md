# 总结

## 背景

- **传统机器学习**：数据集中上传至中心服务器训练，数据源受限，隐私风险高
- **联邦学习 (FL)**：数据留在本地，仅传输模型参数/梯度，缓解了数据隐私问题
- 但 **Non-IID 数据异构** 导致全局模型在不同客户端上效果差异大 → 引出个性化联邦学习 ([PFL](./problems.md#21-数据异构性问题（non-iid）))
- PFL 仍需直面两大痛点：**隐私泄露** 与 **本地表示偏差**

## 挑战

- **数据异构 (Non-IID)**：各客户端数据分布不一致，全局模型难以适配每个客户端
- **隐私泄露**：梯度/特征传输并非绝对安全，[DLG 攻击](./problems.md#22-隐私泄露风险) 可从梯度中重建原始数据
- **本地表示偏差**：PFL 过度个性化，本地模型在未见样本上表现下降，缺乏跨客户端共识引导
- **现有方法局限**：DP 噪声损害模型效用；参数解耦忽略跨客户端信息；简单原型方法未充分处理隐私与噪声
- **核心矛盾**：隐私保护 ↔ 模型效用，二者难以兼得

## 方案

- **FedPKDA**：Privacy-Preserving Knowledge Dynamic Alignment，隐私保护的个性化联邦学习，全流程 客户端 → 服务器 → 客户端
- **模块一：跨客户端原型隐私保护**<br>
  特征裁剪 (Clipping) + 本地类原型 (均值特征) + [拉普拉斯噪声](./math.md#a-差分隐私-ε-界证明)，上传**带噪原型**，提供可量化的 ε-DP 数学保障
- **模块二：服务器端几何滤波**<br>
  [K-Means 聚类](./fedpkda.md#33-模块二-服务器端几何滤波) 获取几何中心，[马氏距离](./utils/ma_distance.md) 衡量偏离程度，反距离**加权聚合**滤除噪声，生成干净全局原型
- **模块三：客户端知识动态对齐**<br>
  双对齐损失
$\mathcal{L}\_{align} = \omega\_{local}(1-\varphi(t))\mathcal{L}\_{LA} + \omega\_{global}\varphi(t)\mathcal{L}\_{GA}$
[时间激活函数 φ(t)](./utils/function.md) 控制权重：**早期偏向本地、后期偏向全局**，缓解表示偏差
- **模型聚合**：Fisher Information Matrix 重要性加权，保护高密度/稀有本地知识不被平均掉
- **效果**：超越 12 个 SOTA 基线——CIFAR-100 (β=0.3) 上 **44.91%** 准确率，超亚军 FedAS **4.23%**；DLG 攻击下 PSNR ≈ 6.9，重建图像 ≈ 随机噪声

> [!TIP]
> 一句话总结：**用带噪原型 + 几何滤波守住隐私，用动态对齐兼顾个性化与全局共识。**

---

[Previous: FedPKDA](./fedpkda.md)
[附录: 数学原理](./math.md)
